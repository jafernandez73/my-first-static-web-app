{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": {
    "build": "echo \"here build the app\" && cp -r src dist",
    "start": "sirv ./src public --cors --single --no-clear --port 8000"
  }
}
